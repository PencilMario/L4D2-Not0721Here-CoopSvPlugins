# Intent

Migrate only special-infected spawn settings from mode VScript reloads into
`Si_SpawnSetting.sp` using Left4DHooks Director-value forwards. Preserve all
public ConVars and commands, and leave mode scripts plus the global script
reloader available for unrelated behavior.
