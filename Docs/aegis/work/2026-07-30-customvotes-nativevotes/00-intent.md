# Intent

Implement the approved CustomVotes NativeVotes hard cutover. Keep ordinary
menus only for selecting a vote and its subject; retire accumulated menu votes
and use the eligible NativeVotes pool for threshold calculation.

Non-goals are replacing selection menus, broadly changing the config format,
or modernizing the whole plugin's SourcePawn syntax.
