# Intent

Add one bounded DeepL fallback after exhausted DeepSeek retries and show optional
all-player progress for unique online translations discovered by the
`OnMapStart` entity scan only.

The cache format, serial queue, DeepSeek retry count, entity writeback, and
console translation behavior must remain compatible.
