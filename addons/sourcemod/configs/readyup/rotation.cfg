"config"
{
	"delay before map change?"				"5"	// The amount of time after an eligible round ends before changing the map.
	"next map is random?"					"1"		// Whether or not the next campaign / map is picked randomly or follows the rotation set in maplist.cfg. (0) Disable (1) Enable.
	"maximum finale fails?"					"1"		// How many times a team may fail the finale attempt before the next map is selected.
	"survival rounds played?"				"1"		// How many times the same survival map is played before the next survival map is selected.
}