# Intent

Implement the approved class restrictions for Mutation 17, Mutation 16, and
Mutation 11 in `Si_SpawnSetting.sp`, refresh them when `mp_gamemode` changes,
and document the engine's unenforced 14-per-class limit.

Non-goals are plugin-side clamping, mutation VScript changes, and behavior
changes for any other game mode.

